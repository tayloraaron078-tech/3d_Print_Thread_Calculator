# 3D Print Thread Calculator (Python)

A small Python app that reproduces the same thread-calculation logic from the provided HTML/JavaScript example.

## Run the app

```bash
python3 thread_calculator.py
```

## Run tests

```bash
python3 -m unittest discover -s tests
```
